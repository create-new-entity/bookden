
server {
    listen 80;
    server_name _; # This nginx will only serve this client. We don't care what the server name is here.

    root /usr/share/nginx/html;
    index index.html;
    
    # If the requested file doesn't exist, fallback to index.html
    location / {
        try_files $uri /index.html;
    }
}
